List interface implementations:

____________________________________________________________
List<String> arrayList = new ArrayList<>();

ѕросто обычный список, максимальное количество еллементов по INT_MAX_SIZE;
при обь€влении можно задать initialCapacity на количество еллементов в списке new ArrayList<>(количество эллементов)
–аботает по типу массива, если надо добавить эллемент в середину то последующие эллементы надо переместить
____________________________________________________________

____________________________________________________________
List<String> linkedList = new LinkedList<>();

—делано в виде ссылок (NODE) на следующий и предидущий эллемент.
дл€ того что бы найти еллемнт надо искать с самого начала. дл€ того что бы вставить в середину нужно мен€ть ссылки
на след и предидущий эллемент.
_____________________________________________________________

_____________________________________________________________
List<String> vector = new Vector<>();

ѕочти тоже самое что и LinkedList но сделано дл€ безопасног использовани€ в многопоточных приложени€х!
_____________________________________________________________

_____________________________________________________________
List<String> stack = new Stack<>();

Last in first out стек. “оже безопасный в отношении потоков. ≈кстендитс€ от Vector
_____________________________________________________________

List<String> synclist = new Collections.synchronizedList(new ArrayList<>());


---------------------------------------------------------------------------------------------

ƒл€ того что бы сортировать обычные примитивные типы или нет, не свои обьекты можно использовать:
Collections.sort(arrayList);

если сравнивать свои обьекты то надо что бы эти обьекты имплементировали интерфейс Comparable

----------------------------------------------------------------------------------------------

Ѕинарный поиск по коллекци€ должен быть только по отсортированной коллекции!!!
Collections.binarySearch(listName, key); // возвращает индекс эллемента

----------------------------------------------------------------------------------------------

ѕриведение листа к массиву:
String[] strings = new String[list.size()];
list.toArray (strings);

ѕриведение массива к листу:
List<String> list = new ArrayList<> ();
list = Arrays.asList (strings);

----------------------------------------------------------------------------------------------

list.equals();

----------------------------------------------------------------------------------------------

ƒобавить эллементов больше чем INT MAX_VALUE можно только в LinkedList но эт не точно))