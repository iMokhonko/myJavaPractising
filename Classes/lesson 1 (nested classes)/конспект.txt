ENUM:
Перечисления обьектов, сущеностей
для поля с ограниченым числом вариантов

.values() - возвращает список или массив все значений в Enum классе, только имена переменных
.valueOf(String name) - возвращает из конструктора значения переменной